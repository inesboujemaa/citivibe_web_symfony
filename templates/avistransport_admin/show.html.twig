{% extends 'base.html.twig' %}

{% block title %}Avistransport{% endblock %}

{% block body %}
    <h1>Avistransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idavis</th>
                <td>{{ avistransport.idavis }}</td>
            </tr>
            <tr>
                <th>Avis</th>
                <td>{{ avistransport.avis }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ avistransport.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avistransport_admin_index') }}">back to list</a>

    <a href="{{ path('app_avistransport_admin_edit', {'idavis': avistransport.idavis}) }}">edit</a>

    {{ include('avistransport_admin/_delete_form.html.twig') }}
{% endblock %}
